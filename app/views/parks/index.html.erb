<h1 class="text-center">Parks that you can visit</h1>

<h3 class="text-center">You may enjoy ...</h3>
<div class="container">
	<% @parks.each do |p| %>
		<div class="alert alert-success">
			<a href="/parks/<%= p.id %>">
				<%= p.name %>
			</a>
		</div>
	<% end %>
</div>